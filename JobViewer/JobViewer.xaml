<UserControl x:Class="JobViewer.JobViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JobViewer"
             mc:Ignorable="d" Height="Auto" Width="Auto"
             >
    <UserControl.Resources>
        <Style x:Key="ScrollBarTrackThumb" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Grid x:Name="Grid">
                            <Rectangle HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Width="Auto"
                Height="Auto"
                Fill="Transparent">
                            </Rectangle>
                            <Border x:Name="CornerScrollBarRectangle"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                CornerRadius="2"
                Width="Auto"
                Height="Auto"
                Margin="0,1,0,1"
                Background="{TemplateBinding Background}">
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Tag" Value="Horizontal">
                                <Setter TargetName="CornerScrollBarRectangle" Property="Width" Value="Auto" />
                                <Setter TargetName="CornerScrollBarRectangle" Property="Height" Value="6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Foreground" Value="#858585"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Width="3" Background="{TemplateBinding Background}" Margin="0,0,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="10" />
                                <RowDefinition Height="100*" />
                                <RowDefinition Height="10" />
                            </Grid.RowDefinitions>
                            <Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="True" Focusable="False">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb" Background="{TemplateBinding Foreground}" Style="{DynamicResource ScrollBarTrackThumb}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton x:Name="PageUp"
                    Command="ScrollBar.PageDownCommand"
                    Opacity="0"
                    Focusable="False" />
                                </Track.IncreaseRepeatButton>
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton x:Name="PageDown"
                    Command="ScrollBar.PageUpCommand"
                    Opacity="0"
                    Focusable="False" />
                                </Track.DecreaseRepeatButton>
                            </Track>
                            <Button x:Name="btnPageUp" Grid.Row="0" Margin="-2,0,-2,0" Background="Transparent" BorderThickness="0" Visibility="Hidden" Command="ScrollBar.PageUpCommand">
                                <Grid>
                                    <Polygon Points="0.5,6 4.5,0 8.5,6 0,6" Fill="#858585" Stroke="#858585"></Polygon>
                                </Grid>
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Height" Value="12"/>
                                                <Setter Property="Width" Value="12"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button x:Name="btnPageDown" Grid.Row="2" Margin="-2,0,-2,0" Background="Transparent" BorderThickness="0" Visibility="Hidden" Command="ScrollBar.PageDownCommand">
                                <Grid>
                                    <Polygon Points="0.5,0 4.5,6 8.5,0 0,0" Fill="#858585" Stroke="#858585"></Polygon>
                                </Grid>
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Height" Value="12"/>
                                                <Setter Property="Width" Value="12"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="GridRoot" Property="Width" Value="7" />
                                <Setter TargetName="btnPageUp" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="btnPageDown" Property="Visibility" Value="Visible"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Thumb" Property="Visibility" Value="Collapsed" />
                            </Trigger>

                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="GridRoot" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="PART_Track" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90" />
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Width" Value="Auto" />
                                <Setter Property="Height" Value="7" />
                                <Setter TargetName="Thumb" Property="Tag" Value="Horizontal" />
                                <Setter TargetName="Thumb" Property="Margin" Value="0,1,0,0" />
                                <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageLeftCommand" />
                                <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageRightCommand" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border BorderBrush="Black" BorderThickness="0.25,0.25,0.25,0.25">
            <DataGrid x:Name="dgStatus" Grid.Row="2" ColumnWidth="*" 
                  RowHeaderWidth="0"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  BorderThickness="0,0,0,0"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  Focusable="False"
                  SelectionUnit="FullRow"
                  PreviewKeyDown="dgStatus_PreviewKeyDown" Background="White">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="Q">
                                <Setter Property="Background" Value="LightYellow"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="R">
                                <Setter Property="Background" Value="LightGreen"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Check Job" Click="CheckJob_Click">
                            <MenuItem.Icon>
                                <Image Source="Icons/Check_File.png" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Cancel Job" Click="CancelJob_Click">
                            <MenuItem.Icon>
                                <Image Source="Icons/Delete.png" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="Refresh" Click="Refresh_Click">
                            <MenuItem.Icon>
                                <Image Source="Icons/Refresh.png" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="View Details" Click="ViewDetails_Click">
                            <MenuItem.Icon>
                                <Image Source="Icons/Check_File.png" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" IsReadOnly="True" Binding="{Binding JobID}" CanUserSort="False" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Job Name" Binding="{Binding JobName}" IsReadOnly="True" CanUserSort="False" Width="5*"/>
                    <DataGridTextColumn Header="User Name" Binding="{Binding UserName}" IsReadOnly="True" CanUserSort="False" Width="3*" MinWidth="75"/>
                    <DataGridTextColumn Header="Tokens" Binding="{Binding Token}" IsReadOnly="True" CanUserSort="False" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Status" IsReadOnly="True" Width="1.75*" CanUserSort="False" MinWidth="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Status}" Value="Q">
                                        <Setter Property="Text" Value="Queue" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="R">
                                        <Setter Property="Text" Value="Running" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!--<DataGridTemplateColumn Header="Process" CanUserSort="False" Width="1.75*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ProgressBar Minimum="0" Maximum="100" Value="{Binding Process}">
                            </ProgressBar>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>-->

                    <DataGridTextColumn Header="TimeLapse" Binding="{Binding TimeElapse}" IsReadOnly="True" CanUserSort="False" Width="75" >
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
